SECRET_KEY=zagro-dev-secret-key-change-in-prod
FLASK_ENV=development
# DATABASE_URL is only needed in production (e.g. Railway PostgreSQL URL)
# Leave unset locally — app will use SQLite automatically
